// Package discovery will hold the x402 v2 Bazaar descriptor, a generic submit
// client (POST a descriptor to any bazaar-shaped index URL), the Registrar
// interface, and the multiplexer. The x402 v2 discovery shape is a shared wire
// format, so it lives in core; rails contribute only their accepts entries.
// Placeholder; design in the Seller blueprint note.
package discovery
